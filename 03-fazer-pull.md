# Como atualizar o projeto (Pull)

Antes de começar a trabalhar, atualize seu projeto.

## Atualizar

```bash
git pull
```

## Se houver conflito

```bash
git add .
git commit -m "Resolve conflito"
git push
```

**Dica:** sempre faça `git pull` antes de começar uma atividade.
